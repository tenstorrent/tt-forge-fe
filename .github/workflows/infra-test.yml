name: Infra Test

on:
  workflow_dispatch:
  workflow_call:
  pull_request:
    branches: [ "main" ]
    paths:
        - 'infra/**'
  push:
    paths:
        - 'infra/**'

jobs:

  run-pytest:

    runs-on: ubuntu-latest
    steps:        
        - uses: actions/checkout@v4
        - name: Run tests and code coverage
          run: |
            cd infra
            pip install -r requirements.txt
            pytest \
                --junitxml=pytest.xml \
                --cov-report=term-missing:skip-covered \
                --cov=src \
                test | tee pytest-coverage.txt

        - name: Show reports
          uses: MishaKav/pytest-coverage-comment@main
          with:
            pytest-coverage-path: infra/pytest-coverage.txt
            junitxml-path: infra/pytest.xml
            